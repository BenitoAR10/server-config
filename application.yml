currency:
  url: ${API_URL:https://api.apilayer.com/exchangerates_data/convert}
  key: YEraXsSahmYnVTklREwJ6uvCDwm3VpVS

server:
  port: 8080

spring:
  datasource:
    type: org.postgresql.jdbc2.optional.SimpleDataSource
    username: ${USER_NAME:postgres}
    password: ${PASSWORD:mysecretpassword}
    url: ${URL:jdbc:postgresql://localhost:5431/postgres}
  config:
    import: optional:file:.env[.properties]
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: currency-converter

keycloak:
  auth-server-url: http://localhost:8080
  autodetect-bearer-only: true
  credentials:
    secret: 8xyJriTCOcipybVGLhXSa1H5W5qCvVoD
  realm: software
  resource: backend
  ssl-required: external
  use-resource-role-mappings: false
  security-constraints:
    - auth-roles:
        - ADMIN
      securityCollections:
        - name: SERVICIOS DEL ADMINISTRADOR
          patterns:
            - /api/currency/list
